$kind: http-request
name: Delete empty room — expect 204
url: '{{baseUrl}}/rooms/LAB-EMPTY'
method: DELETE
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status is 204', function () { pm.response.to.have.status(204); });
order: 11881
